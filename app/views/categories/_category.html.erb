<div id="<%= dom_id category %>" class="category-par">
<div class="cat_wrap">
  <div class="icon_container"><img src="<%= category.icon %>" class="w-24"></div>
  <div class="cat_name"><%= category.name %></div>
</div>
  <div class= "cat_name">$<%= category.total_amount%></div>
</div>
